{
  "name": "todo-app-monorepo",
  "version": "1.0.0",
  "description": "TO-do App - Foco, Metas e Estudo com Lousa Virtual",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd web && npm run dev\" \"cd api && npm run dev\"",
    "build": "npm run build:web",
    "build:web": "cd web && npm install && npm run build",
    "start": "cd api && npm install && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
